import type { Beat } from "../../../scripts/cut-clips";

/**
 * СЦЕНАРИЙ «Картина» — финал «Кремниевой долины» s01e05 «Signaling Risk»
 * (26:49–27:19): Гэвин Белсон в холле Hooli оценивает купленную за полмиллиона
 * картину Чуя — ту самую, что Эрлих забраковал на воротах гаража.
 *
 * Гэвин с гуру перед картиной: «Не слишком вызывающе для офиса?» — «Гэвин, ты
 * достиг всего, бросая вызов эмоциям. Вам ли не знать?» — «Хотя, немного
 * похабно». Дальше сама картина (Динеш-ацтек и статуя Свободы) и сотрудник с
 * карамельным яблоком, который смотрит на неё пустым взглядом и уходит.
 *
 * Пользователь задал точку входа 26:49 сам, поэтому реплика Эрлиха про
 * полмиллиона (26:37–26:46) не взята. Ролик без музыки — по его же просьбе.
 *
 * Драматургия:
 *   хук    — «Не слишком вызывающе для офиса?» крупным планом;
 *   ответ  — «Ты достиг всего, бросая вызов эмоциям. Вам ли не знать?»;
 *   вердикт — «Хотя, немного похабно»;
 *   показ  — картина целиком (режим `view`: на всю ширину, полосы размыты);
 *   панч   — пустой взгляд сотрудника с яблоком, разворот, ушёл.
 *
 * ЦЕНЗУРА. Пах Динеша на картине замазан `blur` — по требованию пользователя
 * и правил площадки. Координаты сняты по увеличенному кадру рипа (не по
 * скрину пользователя: у него другая кадрировка) с запасом на дрейф камеры
 * ≤ 1 % ширины: x 0.278–0.372, y 0.52–0.66 исходного кадра.
 *
 * КАРТИНА В ВЕРТИКАЛИ. Она занимает 0.15–0.64 ширины кадра, вертикальное окно
 * — 0.32: панорамой по ней читалась бы по частям, а гэг требует всей
 * композиции разом. Поэтому бит идёт в режиме `view` (0.6 ширины кадра на всю
 * ширину вертикали, сверху и снизу размытая копия кадра). Увеличение при этом
 * ×2.6 против обычных ×4.8 — пикселей меньше, а не больше.
 *
 * ИНДЕКС ПРАВИЛСЯ: q0555 «Кевин» → «Гэвин» (whisper обеих версий тянет к
 * частому имени, Gemini на секундном фрагменте слышит «Гэвин», в дубляже его
 * так и зовут — q0268, q0305); q0557 «похабное» → «похабно» (large-v3 и Gemini
 * против turbo). Фантомного «Спасибо» на 1621–1623 нет (Gemini: только фон).
 *
 * ГРАНИЦЫ. Склейки: 1609.47 · 1616.31 · 1632.43 · 1636.39 · 1640.10 (титры).
 * Наружный план 1616.3–1632.4 — один непрерывный: мезонин, потом медленный
 * спуск камеры по фасаду на сотрудника, 11 c без слов. В первой версии он
 * был вырезан (1621.9 → 1632.4), но под ним нарастает музыка сцены, и на
 * стыке она рвалась на полуфразе — вернул целиком. Конец — 1638.6: сотрудник
 * уже отвернулся, но ещё целиком в кадре (на 1639.5 он выходит из кадра, на
 * 1640.1 титры).
 *
 * ЗВУК ОДНОЙ ДОРОЖКОЙ. На этом ролике вскрылось, что Remotion на границе
 * каждого `<Sequence>` теряет 20–50 мс звука `<OffthreadVideo>` — на стыках
 * слышен провал, на непрерывной музыке щелчок. `npm run cut` теперь собирает
 * `audio.m4a` (сегменты исходника встык, единый loudnorm), композиция играет
 * его пропом `audioFile`, клипы немые.
 *
 * Сборка:  npm run track -- ValleyMural  →  npm run cut -- ValleyMural
 */
const FILM = "silicon-valley-s01e05";

export const FPS = 24000 / 1001;

export const SCRIPT: Beat[] = [
  // --- Хук: Гэвин и гуру ---------------------------------------------------
  {
    id: "m01",
    film: FILM,
    // Один план (1609.47–1616.31), двое крупно. Старт через 0.05 c после
    // склейки, конец в зазоре перед следующей.
    start: 1609.52,
    end: 1616.3,
    kind: "quote",
    subs: "source",
    // Двойной план крупно: Гэвин на x≈0.27 (лицо 0.155 ширины), гуру на 0.49
    // (0.135) — головы вместе занимают 0.40 ширины, окно 0.32 не вмещает ни
    // при каком центре, а вести по говорящему нельзя: второй остаётся у края
    // половиной лица. Поэтому `view`: 0.46 ширины кадра (0.145–0.605) на всю
    // вертикаль, оба целиком, увеличение ×3.3 вместо ×4.8.
    view: { x: 0.375, width: 0.46 },
    note: "«Не слишком вызывающе для офиса?» — «Гэвин, ты достиг всего, бросая вызов эмоциям»",
  },

  // --- Вердикт и спуск камеры: снаружи за стеклом ------------------------
  {
    id: "m02",
    film: FILM,
    // Один непрерывный план 1616.31–1632.43: Гэвин и гуру на мезонине («Вам
    // ли не знать?» — «Хотя, немного похабно», до 1621.5), затем камера
    // медленно спускается по стеклянному фасаду (1624.8–1628.8 — только
    // стекло) и снизу в кадр въезжает сотрудник с яблоком (1628.8–1632.4).
    // Спуск оставлен целиком: под ним нарастает музыка сцены, и монтажный
    // прыжок 1621.9 → 1632.4 рвал её на полуфразе (заметил пользователь).
    // Старт на 0.03 c ПОЗЖЕ склейки (1616.313): с границей 1616.3 ffmpeg
    // отдавал последний кадр старого плана — у AVI метки кадров плавают.
    start: 1616.34,
    end: 1632.41,
    kind: "quote",
    subs: "source",
    note: "«Вам ли не знать?» — «Хотя, немного похабно» → спуск камеры → сотрудник с яблоком",
    // Ручная траектория. Лица за стеклом мелкие (0.045 ширины) и стоят в
    // 0.11 друг от друга — оба помещаются в одно окно с центром 0.36, но
    // трекер прыгал между ними и прохожим за спиной (окно «мотало»). Пока
    // камера стоит (до 1623.8, t=7.5) — окно неподвижно; на спуске плавно
    // уходит к 0.55, где снизу появится сотрудник (детектор: x=0.65 на
    // 1628.8 → 0.50 на 1630.8), и держится на нём.
    pan: [
      { t: 0, x: 0.36 },
      { t: 7.5, x: 0.36 },
      { t: 12.4, x: 0.55 },
      { t: 14.4, x: 0.51 },
      { t: 16.07, x: 0.52 },
    ],
  },

  // --- Показ: картина ------------------------------------------------------
  {
    id: "m03",
    film: FILM,
    // План 1632.43–1636.39: картина и спина сотрудника справа (от 0.66
    // ширины). Окно `view` 0.055–0.655 — картина целиком, спина за краем.
    start: 1632.43,
    end: 1636.37,
    kind: "action",
    subs: "source",
    // Камера в плане заперта (30 уникальных кадров из 94 в исходнике),
    // единственное движение — спина сотрудника, и она за краем окна. Без
    // наезда 4 c статики в ленте читаются как зависшее видео.
    view: { x: 0.355, width: 0.6, push: 0.07 },
    blur: [{ x: 0.278, y: 0.52, w: 0.094, h: 0.14 }],
    note: "картина Чуя целиком, пах замазан",
  },

  // --- Панч: пустой взгляд -------------------------------------------------
  {
    id: "m04",
    film: FILM,
    // План 1636.39–1640.10: сотрудник с яблоком смотрит, на 1638.3
    // отворачивается и уходит. Конец, пока он целиком в кадре.
    start: 1636.37,
    end: 1638.6,
    kind: "action",
    subs: "source",
    note: "пустой взгляд, разворот, ушёл",
  },
];
