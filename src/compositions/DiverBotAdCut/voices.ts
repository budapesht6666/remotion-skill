/**
 * Голоса второго монтажа — те же, что у основного ролика.
 *
 * Файл существует не ради содержимого, а ради контракта: `npm run vo` ищет
 * `voices.ts` в папке композиции (`scripts/voice-over.ts`), поэтому реэкспорт
 * обязателен. Копировать определение голоса сюда было бы хуже, чем сослаться:
 * два ролика одного продукта обязаны звучать одним диктором, а две копии
 * настроек разъедутся на первой же правке стабильности или темпа.
 */
export { VOICES } from "../DiverBotAd/voices";
export type { VoiceDef, VoiceId } from "../DiverBotAd/voices";
