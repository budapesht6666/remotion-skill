/**
 * Фирменный стиль diverBot — перенос дизайн-системы продукта в ролик.
 *
 * Значения СКОПИРОВАНЫ из `diverBot/promo/src/styles/tokens.css`, а не подобраны
 * на глаз. Реклама встык с сайтом, куда она ведёт: любое расхождение по зелёному
 * или по радиусу карточки читается как «ролик делали не они».
 *
 * Главное правило системы, которое ролик обязан соблюдать так же строго, как
 * сайт (цитата из tokens.css): «База монохромная: чёрный текст на белом.
 * Насыщенный цвет означает данные (рост/падение) или действие (кнопку) — и
 * больше ничего». Поэтому в светлой половине ролика ровно одна крупная заливка
 * цветом — кнопка на финальной плашке. Всё остальное чёрно-белое.
 *
 * Тёмная половина живёт по палитре мини-аппки (её тёмная тема), а не по
 * инвертированному сайту: в кадре появляется настоящий скриншот аппки, и фон
 * вокруг него обязан совпасть с ним, иначе телефон «вырежется» из кадра.
 */

export const BRAND = {
  /** Светлый мир — промо-сайт. */
  ink: "#0a0a0b",
  paper: "#ffffff",
  surface: "#f4f4f6",
  surfaceSunken: "#ececef",
  textMuted: "rgba(10, 10, 11, 0.62)",
  textFaint: "rgba(10, 10, 11, 0.58)",
  border: "rgba(10, 10, 11, 0.12)",
  borderStrong: "rgba(10, 10, 11, 0.35)",

  /** Данные: рост, падение, зона перегрева. */
  bull: "#1a9c53",
  bear: "#d4383d",
  warm: "#d97706",

  /** Действие. Единственная крупная заливка цветом на весь светлый мир. */
  cta: "#088045",
  ctaInk: "#ffffff",

  /** Тёмный мир — тёмная тема мини-аппки. */
  darkPaper: "#121216",
  darkInk: "#f2f2f5",
  darkSurface: "#1b1b21",
  darkMuted: "rgba(242, 242, 245, 0.55)",
  /** Холодная подсветка ночных кадров: ими же тонируются клипы из Gemini. */
  darkTint: "#0b1220",
} as const;

/** Единственная кривая на весь сайт: резкий старт, долгое мягкое оседание. */
export const EASE_OUT = [0.16, 1, 0.3, 1] as const;

export const RADIUS = {
  sm: 8,
  /** Карточки. Тот же радиус, что у карточек сайта и строк аппки. */
  md: 14,
  lg: 24,
  pill: 999,
} as const;

/**
 * Кегли под вертикальный кадр 1080×1920. На сайте они заданы в `clamp()` от
 * ширины окна; здесь ширина известна, поэтому числа зафиксированы — но
 * пропорции между уровнями сохранены, как и правило «в больших кеглях шрифт
 * тонкий (300), потому что жирный в таком размере кричит».
 */
export const TYPE = {
  display: 96,
  h2: 72,
  h3: 44,
  lead: 36,
  body: 32,
  small: 26,
  micro: 22,
} as const;

export const WEIGHT = {
  light: 300,
  regular: 400,
  medium: 500,
  semibold: 600,
} as const;

export const TRACKING = {
  display: "-0.035em",
  heading: "-0.025em",
  normal: "0",
  /** Надзаголовки и пилюли идут вразрядку капителью. */
  micro: "0.08em",
} as const;

/** Реквизиты продукта. Держим в одном месте: они попадают в кадр дословно. */
export const PRODUCT = {
  name: "diverBot",
  site: "diverbot.app",
  handle: "@diver_p1gog_bot",
  priceUsd: 10,
  subscriptionDays: 30,
  trialDays: 3,
  /**
   * Дисклеймер с подвала сайта. В ролике обязателен: он и юридическая страховка,
   * и то, что отличает честную рекламу сигналов от обещания дохода.
   */
  disclaimer:
    "Not financial advice. Market signals describe what has happened on the chart, not what will happen next.",
} as const;
