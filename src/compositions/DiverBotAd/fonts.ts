/**
 * Шрифт рекламного ролика — Inter в весах фирменного стиля diverBot.
 *
 * Отдельный модуль, а не строчка в общем `src/lib/fonts.ts`: там загружены
 * веса 600–800, и ролику их мало. Ему нужен **300** — по правилу дизайн-системы
 * продукта «в больших кеглях тонкий шрифт выглядит дорого, жирный — кричит»
 * все дисплейные заголовки сайта набраны светлым начертанием. Дописать эти
 * веса в общий модуль значило бы тянуть их в каждую композицию проекта:
 * загрузка там происходит на уровне модуля, и импорт файла сам по себе
 * запускает скачивание.
 *
 * Кириллица не нужна: ролик англоязычный, а латинского субсета хватает и на
 * `@diver_p1gog_bot`, и на `$10 / 30 days`.
 */
import { loadFont } from "@remotion/google-fonts/Inter";

export const { fontFamily: BRAND_FONT } = loadFont("normal", {
  weights: ["300", "400", "500", "600"],
  subsets: ["latin"],
});
